[← Назад к Линейным алгоритмам](obsidian://advanced-uri?vault=Obsidian%20Vault&filepath=Спортивное%20программирование%2FLinear%20algorithm.md)

# Максимальный подмассив

## Максимальный подмассив
* **Суть понятия:**
	* Время работы алгоритма линейно зависит от объема входных данных $n$.
	* Масштабируемость: если объем входных данных увеличивается в $X$ раз, то количество элементарных операций и время выполнения программы возрастают тоже примерно в $X$ раз.
	* Задача поиска такого непрерывного участка массива, сумма элементов на котором является максимально возможной среди всех комбинаций.
* **Математическое объяснение:**
	* Функция реального количества операций алгоритма $k(n)$ ограничена сверху функцией $C \cdot n$, начиная с некоторого значения $n_0$.
	* Условие: сумма на отрезке выражается как $p_{j+1} - p_i$. Для максимизации этой разности при фиксированном $j$ алгоритм находит и вычитает минимальное значение $p_i$ на префиксе.
	* Задача сводится к поиску оптимальной пары элементов, примененному к массиву префиксных сумм $p$.
* **Поведение при больших n:**
	* При росте $n$ до миллиона элементов алгоритм $O(n)$ выполнит порядка миллиона операций.
	* В отличие от неэффективных алгоритмов ($O(n^2)$), линейные алгоритмы отлично подходят для обработки больших массивов данных в реальном времени.
* **Реализация на C++:**
	```cpp
	vector<int> p(n + 1, 0);
	for (int i = 1; i <= n; ++i) {
		p[i] = p[i - 1] + a[i - 1];
	}
	int ibest = 0, jbest = 0, imin = 0;
	for (int j = 1; j < n; ++j) {
		if (p[j] < p[imin]) {
			imin = j;
		}
		if (p[j + 1] - p[imin] > p[jbest + 1] - p[ibest]) {
			jbest = j;
			ibest = imin;
		}
	}
	cout << ibest << " " << jbest << endl;
	```
